<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 수정요망 -->
<mapper namespace="com.lifemenu.dao.qebc.IResveDaoQebc">
	<!-- 
	<resultMap type="#modelname" id="YourResultSet">
		<id property="param1" column="columnname1" />
		<result property="param2" column="columnname2" />
		<result property="param3" column="columnname3" />
	</resultMap>
	
	<select id="getAll" resultMap="YourResultSet">
		SELECT * FROM tablename1
	</select>
	-->
	<select id="selectMultiResve" resultType="resveDto">
		SELECT * FROM RESVE ORDER BY RESVE_CODE
	</select>
	
	<select id="selectOneResveRownum" resultType="resveDto">
		SELECT * FROM (SELECT * FROM RESVE ORDER BY RESVE_CODE DESC) WHERE ROWNUM = 1
	</select>
	
	<select id="selectSelerName" resultType="mberDto">
		SELECT * FROM MBER M, SELER S WHERE M.MBER_CODE = S.MBER_CODE AND S.MBER_CODE = #{mberCode}
	</select>
	
</mapper>
